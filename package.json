{
  "name": "mlt-desktop-connector",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MLT Desktop Connector — local Facebook Marketplace posting agent",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "bash -c 'set -a && [ -f .env ] && . ./.env; set +a && tauri dev'",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.4.0",
    "@tauri-apps/plugin-shell": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
