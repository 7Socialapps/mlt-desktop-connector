{
  "name": "mlt-desktop-connector",
  "private": true,
  "version": "1.0.5",
  "type": "module",
  "description": "MLT Desktop Connector — local Facebook Marketplace posting agent",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "bash -c 'set -a && [ -f .env ] && . ./.env; set +a && tauri \"$@\"' --",
    "tauri:dev": "npm run tauri -- dev",
    "tauri:build": "tauri build",
    "browser:install": "playwright install chromium",
    "browser:detect": "node browser-sidecar/cli.mjs detect",
    "browser:test": "node browser-sidecar/cli.mjs launch-test && node browser-sidecar/cli.mjs close-test",
    "build:staging": "bash scripts/build-staging.sh",
    "build:production": "bash scripts/build-production.sh",
    "test": "node browser-sidecar/facebook-detector.test.mjs && node browser-sidecar/marketplace-evaluator.test.mjs && node browser-sidecar/messenger-evaluator.test.mjs && node browser-sidecar/notifications-evaluator.test.mjs && node browser-sidecar/navigation.test.mjs && node browser-sidecar/vehicle-create-verifier.test.mjs && node browser-sidecar/browser-process.test.mjs && node browser-sidecar/marketplace/form.test.mjs && cd src-tauri && cargo test"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.4.0",
    "@tauri-apps/plugin-shell": "^2.2.0",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
